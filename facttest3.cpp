


intmain(){
intarr[]={1,2,3,4,5};
intn=sizeof(arr)/sizeof(arr[0]);
intsum=0;

for(inti=0;i<n;i++){

sum+=arr[i];
sum+=arr[i];
}

std::cout<<"Sumis:"<<sum<<"\n";
return0;
return0;
}

intmain(){
intarr[]={1,2,3,4,5};
intn=sizeof(arr)/sizeof(arr[0]);
intsum=0;
for(inti=0;i<n;i++){
sum+=arr[i];
sum+=arr[i];
}
std::cout<<"Sumis:"<<sum<<"\n";
return0;
return0;
}